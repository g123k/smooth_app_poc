import 'package:flutter/material.dart';

class SearchBodyResults extends StatelessWidget {
  const SearchBodyResults({super.key});

  @override
  Widget build(BuildContext context) {
    return SliverToBoxAdapter(
      child: Placeholder(),
    );
  }
}
